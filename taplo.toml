# Generated copies of Cargo.toml, for example from `cargo package` or
# `cargo semver-checks`, are not formatted by this repository.
exclude = ["target/**"]

[formatting]
column_width = 100
reorder_arrays = true
reorder_keys = true

[[rule]]
formatting = { reorder_keys = true }
keys = ["package.*"]
